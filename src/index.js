import stateManager, { dispatch } from './state-manager/index';
import domManager, { createElement } from './dom-manager/index'

export default {
  stateManager,
  dispatch,
  domManager,
  createElement
}